services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - uploads_data:/app/uploads
      - db_data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/voice_notes.db

volumes:
  uploads_data:
  db_data:
